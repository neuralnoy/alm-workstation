[package]
name = "alm-core"
version = "0.1.0"
edition = "2024"

[dependencies]
alm-time = { path = "../alm-time" }

chrono = { version = "0.4.45", features = ["serde"] }
rust_decimal = "1.43.0"
serde = { version = "1.0.229", features = ["derive"] }
thiserror = "2.0.20"
uuid = { version = "1.26.1", features = ["serde", "v4"] }

[dev-dependencies]
rust_decimal_macros = "1.40.0"
